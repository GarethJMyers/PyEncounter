from src.Entity.EntityCollection import EntityCollection

class TurnCounter:
    """
    This class tracks the turn number and number of rounds that have elapsed. Also holds the entity collection.
    """
